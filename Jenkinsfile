pipeline {
    agent {label 'docker'}
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'Hello UADB'
                }
            }
        }
    }
}